.env
vendor/
node_modules/
storage/*.key
.phpunit.cache/

# Środowiska efemeryczne przebiegów bramki — nigdy w repozytorium.
.env.bramka.*

# Artefakty pomiarowe perturbacji (XML, TSV, logi). Wynik pomiaru nalezy do
# raportu, nie do repozytorium — i tak jest odtwarzalny skryptem.
/dowody/

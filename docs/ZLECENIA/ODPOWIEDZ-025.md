# ODPOWIEDŹ-025 — materiał przyjęty, odpowiedź merytoryczna jest w `ODPOWIEDZ-020-klasa-3.md`

`ZLECENIE-025` było materiałem, nie pozycją — regułę „kontrola tekstowa nad kodem filtruje
komentarze, albo jej zero jest bezwartościowe" przyjąłem i **zastosowałem tego samego dnia
trzy razy**: przy `SesjaBezJawnychDanychTest` (zamknięty członek klasy 3), przy `SondaPortowTest`
i przy `KlamraSkryptowTest`.

**Twoja hipoteza „może to jeden mechanizm, a nie siedem spraw" sprawdziła się CZĘŚCIOWO** —
rozliczenie w `ODPOWIEDZ-020-klasa-3.md`: jeden mechanizm dla jednej kontroli, dwie inne już
filtrowały, dwie są denylistami, gdzie komentarz daje **awarię głośną**.

Ten plik istnieje wyłącznie po to, żeby pozycja nie wyglądała z kanału na otwartą.

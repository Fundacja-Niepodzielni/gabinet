# PODJĘTO-032 · 09.08.2026 19:34

**Wziąłem: kontrola unieważnienia jako MIDDLEWARE + pomiar okna na ścieżce NAJDŁUŻSZEJ.**

**Źródło (2) i (3) naraz:** własne znalezisko z `ODPOWIEDZ-031` — blok `withMiddleware`
jest pusty, więc zasięg kontroli wyznacza pamięć autora trasy, a nie konstrukcja.

**Dlaczego to, a nie `R6B-8` / `R6B-6` z klasy 3:** tamte dotyczą **przyrządu**, ta —
**dostępu do systemu**. Waga: nowa trasa chroniona bez kontroli unieważnienia. Osiągalność:
stan bieżący, rosnąca z każdą trasą dokładaną w F2.

Zlecenia architekta otwartego brak — `030` to protokół (S-3), `031` odpowiedziane plikiem.

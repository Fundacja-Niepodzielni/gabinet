# PODJĘTO-030 · 09.08.2026 19:22

**Wziąłem: `R6B-2` / `R6A-1` — test „POZYTYWNY" dowodzi czego innego, niż deklaruje.**

**Źródło (2):** własny rejestr, wg iloczynu WAGI i OSIĄGALNOŚCI — kolejny człon klasy 3 po
`R6B-11` i `R6B-7`, wskazany przeze mnie w `ODPOWIEDZ-020-klasa-3` jako trzeci w kolejności.

**Dlaczego to, a nie co innego:** dotyczy **dostępu po wylogowaniu**, a dziś dodatkowo **sam
zmieniłem tę ścieżkę** (naprawa `RejestrSesji`, `ZLECENIE-024`) i w `ODPOWIEDZ-020-klasa-3`
zapisałem, że **wpływu tej naprawy na świat tego testu NIE zmierzyłem**. To jest jednocześnie
człon klasy 3 i mój własny nazwany dług — dwa źródła wskazują to samo.

Zlecenia architekta otwartego brak (028 i 029 to protokół i decyzja, obie zapisane).
